eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
server:
  port: 9011
spring:
  application:
    name: feign
feign:
  hystrix:
    enabled: true
management:
  security:
    enabled: false
