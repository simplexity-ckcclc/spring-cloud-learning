spring:
    cloud:
        config:
          discovery:
            enabled: true
            service-id: config-server
#          uri: http://localhost:9030/
          label: master
          name: feign
          profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/